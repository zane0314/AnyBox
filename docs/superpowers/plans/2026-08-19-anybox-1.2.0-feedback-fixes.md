# AnyBox 1.2.0 Feedback Fixes Implementation Plan

**Goal:** 修复 1.2.0 的应用策略继承/分应用开关、竞速参数可见性，以及 ASN/国外规则启用失败。

**Architecture:** 复用现有 DataStore、AppListActivity、SmartRoutingStore 和 ConfigBuilderKt。先通过设备 UI 与 logcat 取得真实错误，再在共享调用点做最小修复；不增加新的应用集合或规则存储。

**Tech Stack:** Android APK 反编译工程、smali、apktool、adb、sing-box。

## Global Constraints

- 以 apktool_out 为权威修改目录。
- 不清理用户数据库，不 reset/checkout，不提交或推送。
- 修复前完成根因调查，修复后全量构建、签名、安装和 VPN 冒烟验证。

### Task 1: 复现并定位

- [ ] 在 emulator-5554 记录设置页 UI hierarchy、应用选择器入口和 ASN/国外规则原始错误。
- [ ] 将错误映射回规则文本/出口/Preference 数据流，写入 .ai/HANDOFF.md。

### Task 2: 最小修复

- [ ] 修复普通分应用代理入口与智能策略选择器的交互分支，保留两套数据用途。
- [ ] 使智能策略首次无自定义选择时从现有 VPN 应用集合初始化。
- [ ] 让竞速设置在设置页以中文可见标题出现，并确认实际加载。
- [ ] 修复 ASN/国外规则的合法 sing-box 配置生成或明确拒绝不支持的规则输入。

### Task 3: 验证

- [ ] git diff --check。
- [ ] apktool 全量构建、zipalign、apksigner。
- [ ] 覆盖安装不清理数据；验证设置、应用策略、竞速参数和规则开关。
- [ ] VPN 连接/停止，扫描崩溃与配置错误日志。
