# AnyBox 1.1.2 实施计划

日期：2026-08-17

基线：AnyBox 1.1.1（238）

目标：AnyBox 1.1.2（239），包名继续使用 `com.zane.proxy`。

实施原则：只复用现有 `RuleEntity`、`SmartRoutingStore`、`SmartRoutingTargetHelper`、`SmartRoutingRuleHelper`、`SmartRoutingRuleConfigHelper`、`rulesUpdateInterval` 和现有服务生命周期，不新增数据库表、第二套规则系统、第二套更新周期或新依赖。

## Task 1：固化基线与回退点

**Files:**

- Modify: `.ai/HANDOFF.md`（Git 忽略，仅用于交接）

- [ ] 记录当前分支、提交、版本、包名和工作区状态。
- [ ] 创建明确的临时回退快照，只包含本轮会修改的文件。
- [ ] 对仓库内 `apktool_out/` 执行一次强制全量构建，确认 1.1.1 基线可汇编。
- [x] 检查可用的 Android 设备、签名工具、现有 keystore 和远端私人仓库，不修改外部状态。

验证：`apktool b -f apktool_out` 退出码为 0；`git diff --check` 通过。

## Task 2：普通路由逐条前置/后置与应用内展开

**Files:**

- Modify: `apktool_out/smali/io/nekohasekai/sagernet/database/RuleEntity.smali`
- Modify: `apktool_out/smali/io/nekohasekai/sagernet/ui/RouteSettingsActivity.smali`
- Modify: `apktool_out/smali/io/nekohasekai/sagernet/ui/SmartRoutingFragment.smali`
- Modify: `apktool_out/res/xml/route_preferences.xml`
- Modify: `apktool_out/res/values/arrays.xml`
- Modify: `apktool_out/res/values/strings.xml`
- Modify: `apktool_out/res/values-zh-rCN/strings.xml`
- Modify only where the existing startup path requires it: application/startup smali and PreferenceDataStore key declarations.

- [x] 将 `takesPriorityOverSmartRouting()` 简化为只返回已有布尔字段，删除出口类型强制前置逻辑。
- [x] 将编辑页的开关替换为复用同一 key 的二选一菜单：前置=true，后置=false；新规则默认后置。
- [x] 移除编辑页按出口隐藏位置控件的逻辑，所有规则都可选择位置。
- [x] 在首次 1.1.2 启动时执行幂等迁移：旧规则 `outbound != 0` 写为前置，`outbound == 0` 保持原值；事务成功后再写完成标记。
- [x] 让智能分流页三点入口复用现有路由卡片展开/收起逻辑，不再打开 `AssetsActivity`。
- [x] 在现有路由列表摘要中显示前置/后置；如当前 adapter 无可复用摘要位，则只在编辑页明确显示，不新建第二套列表。
- [x] 构建并静态检查最终顺序仍为：前置普通路由 → 应用策略组 → 后置普通路由 → 默认出口。

最小检查：构造一条 `outbound != 0, prioritize=false` 规则，确认 `takesPriorityOverSmartRouting()` 返回 false；构造 true 规则返回 true。

## Task 3：应用策略出口与自定义域名

**Files:**

- Modify: `apktool_out/smali/io/nekohasekai/sagernet/routing/SmartRoutingStore.smali`
- Modify: `apktool_out/smali/io/nekohasekai/sagernet/routing/SmartRoutingTargetHelper.smali`
- Modify: `apktool_out/smali/io/nekohasekai/sagernet/routing/SmartRoutingRuleConfigHelper.smali`
- Modify: `apktool_out/smali/io/nekohasekai/sagernet/ui/SmartRoutingFragment.smali`
- Modify: `apktool_out/res/layout/layout_smart_routing_item.xml`
- Modify: `apktool_out/res/values/arrays.xml`
- Modify: `apktool_out/res/values/strings.xml`
- Modify: `apktool_out/res/values-zh-rCN/strings.xml`
- Create only if the existing UI helper pattern requires it: one focused SmartRouting domain UI helper smali class.

- [x] 先确认 1.1.1 是否已完整支持 `direct` 与 `reject` token；只补缺失分支，不重写目标选择器。
- [x] 在 `SmartRoutingStore` 现有 PreferenceDataStore 命名空间保存每个策略的多条自定义域名，不改 Room schema。
- [x] 添加、查看、替换、删除域名规则；输入只填值，类型使用 DOMAIN、DOMAIN-SUFFIX、DOMAIN-KEYWORD，默认 DOMAIN-SUFFIX。
- [x] 保存时 trim、拒绝空值、按“类型+值”去重；删除自定义策略时复用现有清理路径清理其域名。
- [x] 在配置生成阶段把三种类型映射到 sing-box 的 `domain`、`domain_suffix`、`domain_keyword`，并绑定策略已解析的真实 outbound。
- [x] 节点组、具体节点、直连、拒绝、应用、内置规则、远程规则和自定义域名统一指向同一策略出口。

最小检查：保存 `DOMAIN-SUFFIX youtube.com`、`DOMAIN exact.example`、`DOMAIN-KEYWORD google`，确认最终配置三类字段各一项且出口一致。

## Task 4：引用规则手动与自动更新

**Files:**

- Modify: `apktool_out/smali/io/nekohasekai/sagernet/routing/SmartRoutingStore.smali`
- Modify: `apktool_out/smali/io/nekohasekai/sagernet/routing/SmartRoutingRuleHelper.smali`
- Modify: `apktool_out/smali/io/nekohasekai/sagernet/ui/SmartRoutingFragment.smali`
- Modify: `apktool_out/res/layout/layout_smart_routing.xml`
- Modify: `apktool_out/res/xml/global_preferences.xml`
- Modify: `apktool_out/res/values/arrays.xml`
- Modify: `apktool_out/res/values/strings.xml`
- Modify: `apktool_out/res/values-zh-rCN/strings.xml`
- Modify only at the existing VPN-connected lifecycle hook: the current service/state callback smali file.
- Create only if required to keep service/UI changes small: one focused update coordinator smali class.

- [ ] 复用 `rulesUpdateInterval`，将无配置的新安装默认值设为 24 小时；已有 0 或合法自定义值不覆盖。
- [ ] 在智能分流页增加“一键更新全部引用规则”、自动更新间隔和连接后首次检查延迟，沿用现有 Liquid Glass 样式。
- [ ] 记录每个规范化 URL 的最近成功时间；同 URL 去重后只检查一次。
- [ ] 手动更新忽略过期判断；自动更新只处理已过期 URL。
- [ ] `.list` 继续复用现有下载、解析、成功替换缓存、失败保留旧缓存路径。
- [ ] `.srs` 无安全核心刷新入口时只报告“等待核心按更新周期刷新”，不伪报成功。
- [ ] 每次 VPN 连接成功后按用户设置延迟检查；默认 30 秒。自动间隔为 0 时不调度。
- [ ] VPN 长连接时复用现有协程/服务作用域按同一间隔继续检查；服务停止时自动取消，不增加常驻线程。
- [ ] 多条规则完成后最多触发一次现有配置重载；若无安全重载入口，则保留缓存并从下次连接生效。

最小检查：两个策略引用同一 URL 时只下载一次；一个有效 `.list` 与一个失败 URL 并存时，有效缓存更新、失败旧缓存保留、VPN 不停止。

## Task 5：版本、构建、签名与运行回归

**Files:**

- Modify: `apktool_out/apktool.yml`
- Modify only if仍残留旧品牌文本: Android resources/manifest 中的应用显示名。
- Produce: `/Users/zane/Downloads/AnyBox_1.1.2.apk`

- [ ] 将版本改为 `1.1.2 (239)`，包名和应用数据目录不变。
- [ ] 扫描并移除临时 URL、诊断 JSON、测试节点和调试导出代码。
- [ ] 强制全量构建、zipalign、使用 1.1.1 同一签名签名并验证 v1/v2/v3。
- [ ] 若有设备，覆盖安装前备份数据库和关键 Preference；使用 `adb install -r --no-incremental`，不得卸载或清数据。
- [ ] 核对迁移仅改变预期的路由位置字段和迁移标记；节点、节点组、普通路由、智能策略、应用选择、规则 URL/缓存、自定义域名和分应用代理数据全部保留。
- [ ] 真实启动 VPN，检查最终 sing-box JSON 和流量路径，不只检查 UI。
- [ ] 检查日志无 FATAL EXCEPTION、VerifyError、ClassCastException、NoSuchMethodError、IllegalAccessError。
- [ ] 记录 APK 大小和 SHA-256。

## Task 6：整理提交并推送私人仓库

**Files:**

- Modify: `.ai/HANDOFF.md`（Git 忽略，仅用于交接）
- Commit: 本轮源码、资源、文档与可重复验证记录；不提交 APK、keystore、数据库备份或敏感日志。

- [ ] `git diff --check`，确认无无关改动。
- [ ] 按已验证工作单元提交，最终提交消息明确版本和功能范围。
- [ ] 使用 verification-before-completion 门禁复查构建、签名、版本、数据保留和运行证据。
- [ ] 推送到现有 GitHub 私人仓库 `zane0314/AnyBox` 的 `codex/anybox-1.1.2` 分支；不得公开仓库。

完成标准：本地交付物为 `AnyBox_1.1.2.apk`，真实配置和运行验证通过，私人远端分支包含全部已验证代码和设计/计划。
